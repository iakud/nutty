#include <catta/net/Buffer.h>

using namespace catta;

ssize_t SendBuffer::write(Socket& socket) {

	return 0;
}

ssize_t ReceiveBuffer::read(Socket& socket) {
	// struct iovec vec[2];

	return 0;
}